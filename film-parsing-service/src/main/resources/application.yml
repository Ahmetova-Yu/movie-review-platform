server:
  port: 8084

spring:
  application:
    name: film-parsing-service
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: mySecretKeyForJWTTokenGeneration2024VeryLong

logging:
  level:
    com.moviereview: DEBUG